---
import { Home, Search, Library } from 'lucide-react';
---

<aside class="bg-zinc-900 flex flex-col h-full rounded-lg overflow-hidden">
  <div class="p-6 flex flex-col gap-6">
    <div class="flex items-center gap-2 text-white font-bold text-xl mb-4">
      <span>NotSpotify🎵</span>
    </div>

    <nav class="flex flex-col gap-4">
      <a href="/" class="flex items-center gap-3 text-zinc-400 hover:text-white transition-colors">
        <Home size={24} />
        <span class="font-medium">Inicio</span>
      </a>
      <a href="/search" class="flex items-center gap-3 text-zinc-400 hover:text-white transition-colors">
        <Search size={24} />
        <span class="font-medium">Buscar</span>
      </a>
      <a href="/library" class="flex items-center gap-3 text-zinc-400 hover:text-white transition-colors">
        <Library size={24} />
        <span class="font-medium">Tu Biblioteca</span>
      </a>
    </nav>
  </div>
  
  <div class="flex-1 bg-zinc-900/50 p-4 m-2 rounded-lg">
    <p class="text-xs text-zinc-500 uppercase font-bold">Playlists</p>
  </div>
</aside>